{
  "name": "south-africa-regex",
  "version": "1.1.5",
  "description": "useful regex functions for south africa",
  "type": "commonjs",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "clean": "shx rm -rf dist/*",
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "mocha --ui qunit",
    "testall": "mocha --ui qunit dist/test",
    "prepack": "npm run clean && npm run build"
  },
  "keywords": [
    "regex",
    "south",
    "africa",
    "tax",
    "number",
    "za"
  ],
  "author": "matthew fainman",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "shx": "^0.3.3",
    "@types/node": "^14.14.10",
    "typescript": "^4.1.2"
  }
}
